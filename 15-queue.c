#include "monty.h"

/**
 * queue - Sets the format of the data to a queue (FIFO)
 * @stack: A pointer to the stack.
 * @line_number: Line number where the queue function is called.
 */

void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	args->stack = 0;
}
